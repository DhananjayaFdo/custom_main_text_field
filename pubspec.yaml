name: custom_main_text_field
description: A highly customizable Flutter text field with password visibility toggle, prefix/suffix icons, validation, focus states, and extensive styling options.
version: 1.0.1
# TODO: Update before publishing
homepage: https://github.com/DhananjayaFdo/custom_main_text_field
repository: https://github.com/DhananjayaFdo/custom_main_text_field
issue_tracker: https://github.com/DhananjayaFdo/custom_main_text_field/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

topics:
  - text-field
  - input
  - form
  - validation
  - password
